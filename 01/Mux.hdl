/**选择器
 * 
 * if sel = 0 then out = a else out = b
 * 
 */
CHIP Mux {
    IN a,b,sel;
    OUT out;

    PARTS:
 	Not(in=sel,out=out1);
 	And(a=a,b=out1,out=o1);
 	And(a=sel,b=b,out=o2);
 	Or(a=o1,b=o2,out=out);
}
